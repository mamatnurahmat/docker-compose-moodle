define(['jquery', 'core/ajax', 'local_edwiserform/fastsearch', 'local_edwiserform/fastselect'], function ($, ajax) {
    return {
        has_required_fields: function() {
            return true;
        }
    };
});